const initialState = {
  users: [],
  user: {},
  isLoading: false,
  error: null | "",
};

export const useReducer = (state = initialState, action) => {
  switch (action.type) {
    case value:
      break;

    default:
      return state;
  }
};
